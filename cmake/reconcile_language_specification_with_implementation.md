# Language specification reconciliation receipt

The Part One language specification now explicitly records its conformance
status. Normative behavior covers file-local opt-in, nested fields, required
profiles, explicit effects, the expert-control hierarchy, ordinary C++
fallthrough, structured errors, and implementation-defined target costs.

No deviation was silently normalized: “proposed” remains the mutable governance
label, while general JIT compilation stays deferred to Part Two.
