# Programming guide reconciliation receipt

The developer guide is pinned to the Part One AOT surface. Its examples span
minimal source, profiles, planning, realization, custom passes, unsafe native
paths, LTO, SDK use, celleratord queries, and ordinary C++ fallback. The focused
gate compiles the complete example inventory and rejects duplicate or missing
families.
