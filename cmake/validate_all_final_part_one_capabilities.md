# Final Part One capability validation

The machine-readable matrix at `docs/compiler/PART_ONE_CAPABILITY_MATRIX.json`
lists every mandatory Part One capability and keeps the two deferred families
explicit. The compiled focused gate rejects omissions and duplicates in the
corresponding frozen acceptance inventory.
