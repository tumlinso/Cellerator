# Minimal reference profile artifacts v1

The checked-in CSV is the canonical deterministic source for a compact test
artifact: two four-element domains, one four-edge relation, and synthetic mean
and variance placeholders. Row and field ordering are fixed. This data exists
only for parser, Sema, IR, fallback-planner, example, and CI coverage and makes
no claim of realistic or optimized biology.
