# Reflection and inline IR helpers v1

`reflection.cell` uses ordinary modern C++ traits, visitors, patterns, builders,
and pass adapters. An inline rewrite is a typed value and callable; no alternate
metaprogramming language or hidden IR authority is introduced.
