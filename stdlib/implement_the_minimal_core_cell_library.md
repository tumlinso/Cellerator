# Minimal core `.cell` library v1

`core.cell` supplies pointer/count views, status-bearing results, and
no-allocation traits. Its declarations are ordinary C++17-compatible `.cell`
source behind file-local opt-in; the compiler recognizes their semantic bridge
roles without granting them privileged syntax or ownership.
