# Profile and persistence helpers v1

Typed names and selectors preserve stable identity. Lifetime facts separate
structure epochs from value generations, mutation contracts state which may
change, and `select_profile` always records explicit selection. There is no
ambient or silently selected biological profile.
