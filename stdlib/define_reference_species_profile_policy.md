# Reference species profile policy v1

The three bundled species profiles contain only audited taxonomy identities and
synthetic test placeholders. Their stable names include `test.v1`, carry source,
version, license, and warning metadata, declare low performance, and forbid
automatic selection. They are CI/parser fallbacks, not optimized biology.
