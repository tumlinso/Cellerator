# Internal executable inventory. Every advertised device target links real code.
function(ru1_test target source)
    add_executable(${target} ${source})
    target_compile_features(${target} PRIVATE cxx_std_17)
    target_compile_options(${target} PRIVATE -UNDEBUG)
    target_include_directories(${target} PRIVATE "${PROJECT_SOURCE_DIR}/include")
    set_target_properties(${target} PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}")
endfunction()
ru1_test(ceRU1HostTests tests/relation_update_spine_v1/calculus_test.cc)
target_link_libraries(ceRU1HostTests PRIVATE Cellerator::relation_calculus)
add_test(NAME ru1_calculus COMMAND ceRU1HostTests --suite calculus)
ru1_test(ceRU1ReferenceTests tests/relation_update_spine_v1/reference_test.cc)
target_link_libraries(ceRU1ReferenceTests PRIVATE Cellerator::relation_semantics)
add_test(NAME ru1_reference COMMAND ceRU1ReferenceTests)
ru1_test(ceRU1CoverTests tests/relation_update_spine_v1/gradient_cover_test.cc)
target_link_libraries(ceRU1CoverTests PRIVATE cellerator_relation_gradient_cover)
add_test(NAME ru1_cover COMMAND ceRU1CoverTests)
ru1_test(ceRU1CompilerTests tests/relation_update_spine_v1/compiler_conformance_test.cc)
target_link_libraries(ceRU1CompilerTests PRIVATE cellerator_relation_update_compiler)
add_test(NAME ru1_compiler COMMAND ceRU1CompilerTests)
foreach(name source_bridge dual_origin)
    ru1_test(ceRU1_${name} tests/relation_update_spine_v1/${name}_test.cc)
    target_link_libraries(ceRU1_${name} PRIVATE cellerator_relation_update_compiler)
    add_test(NAME ru1_${name} COMMAND ceRU1_${name})
endforeach()
if(NOT CELLERATOR_ENABLE_CUDA STREQUAL "OFF")
    # A linked pre-main guard makes the sm70 requirement intrinsic to each test,
    # including retained tests whose original main predates the acceptance flag.
    set(ru1_guard "${CMAKE_CURRENT_BINARY_DIR}/ru1_sm70_guard.cu")
    file(WRITE "${ru1_guard}" "#include <cuda_runtime_api.h>\n#include <cstdio>\n#include <cstdlib>\nnamespace { struct sm70_guard { sm70_guard() { int device=0; cudaDeviceProp p{}; if(cudaGetDevice(&device)!=cudaSuccess || cudaGetDeviceProperties(&p,device)!=cudaSuccess || p.major!=7 || p.minor!=0) { std::fputs(\"actual sm70 GPU required; no skip\\n\",stderr); std::exit(1); } } }; sm70_guard guard; }\n")
    function(ru1_gpu target source direct_pair)
        ru1_test(${target} ${source})
        target_sources(${target} PRIVATE "${ru1_guard}")
        if(direct_pair)
            target_link_libraries(${target} PRIVATE cellerator_relation_update_support)
        else()
            target_link_libraries(${target} PRIVATE cellerator_relation_update_core)
        endif()
    endfunction()
    ru1_gpu(ceRU1GpuNumerics tests/relation_update_spine_v1/gpu_numerics_test.cu FALSE)
    ru1_gpu(ceRU1GpuBindings tests/relation_update_spine_v1/gpu_bindings_test.cu TRUE)
    ru1_gpu(ceRU1GpuLifecycle tests/relation_update_spine_v1/gpu_lifecycle_test.cu FALSE)
    ru1_gpu(ceRU1ReadinessTests tests/relation_update_spine_v1/readiness_adversarial_test.cu FALSE)
    ru1_gpu(ceRU1WmmaLegality tests/relation_update_spine_v1/wmma_legality_test.cu FALSE)
    ru1_gpu(ceRU1N1Regression tests/semantic_spine/native/lifecycle_test.cu TRUE)
    add_test(NAME ru1_gpu_numerics COMMAND ceRU1GpuNumerics --require-sm70)
    add_test(NAME ru1_gpu_bindings COMMAND ceRU1GpuBindings --require-sm70)
    add_test(NAME ru1_gpu_lifecycle COMMAND ceRU1GpuLifecycle --require-sm70)
    add_test(NAME ru1_readiness COMMAND ceRU1ReadinessTests --require-sm70)
    add_test(NAME ru1_wmma_legality COMMAND ceRU1WmmaLegality --require-sm70)
    add_test(NAME ru1_n1_regression COMMAND ceRU1N1Regression --require-sm70)
    foreach(name forward_n16 transpose_n16 sparse_gradient value_update update_publication native_failure)
        ru1_gpu(ceRU1_${name} tests/relation_update_spine_v1/${name}_test.cu TRUE)
        add_test(NAME ru1_${name} COMMAND ceRU1_${name})
    endforeach()
    foreach(name gradient_pack hybrid_gradient readiness_component read_lease)
        ru1_gpu(ceRU1_${name} tests/relation_update_spine_v1/${name}_test.cu FALSE)
        add_test(NAME ru1_${name} COMMAND ceRU1_${name})
    endforeach()
    add_subdirectory(examples/relation_update_spine_v1)
endif()
