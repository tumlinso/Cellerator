# Interoperability ownership

- `baseplane/` consumes frozen Baseplane predicate/event vocabulary without
  making Cellerator a sequence engine.
- `cellshard/` adapts storage/delivery contracts without transferring
  Cellerator execution semantics to CellShard.
- A GlassHelix directory is deliberately absent until its scientific request
  contract is frozen.
