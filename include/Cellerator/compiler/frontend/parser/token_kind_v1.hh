#pragma once

#include <Cellerator/compiler/frontend/parser/freeze_the_executable_grammar_revision_and_token_vocabul_v1.hh>

// Stable public facade for the revision 0.1 token vocabulary.  The underlying
// definitions remain in the grammar-freeze header so existing lane consumers
// and this checkpoint interface share one token_kind identity.
