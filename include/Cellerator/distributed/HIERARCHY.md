# Distributed hierarchy compatibility

The authoritative hierarchy and communication-planning contract lives in
`include/Cellerator/planner/distributed/`. This historical directory is a
forwarding compatibility surface only.
