# Opaque barriers and missed optimizations

Actions identify native-call alias/effect uncertainty, field boundaries, profile widening, missing persistence facts, and canonicalization costs. Only mechanically safe annotations and order-preservation edits are offered as fix-its.
