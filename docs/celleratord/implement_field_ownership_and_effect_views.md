# Field ownership and effects

Cursor queries expose the enclosing and nested field boundaries, captured values, native barriers, reads, writes, observable effects, profile environment, and optimization visibility.
