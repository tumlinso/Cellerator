# Cross-TU LTO benchmark v1

Authorized field chains compare no LTO, conventional materialization, and CEIR
LTO. Records cover object CEIR bytes, extraction, merge, incremental cache,
replanning, re-emission, link wall time, final binary bytes, and independently
validated runtime. Compile/link cost and runtime benefit are reported together;
eleven raw samples execute under the benchmark mutex.
