# Planning and complete-cost benchmark v1

Generic, representative, and adversarial profiles measure decomposition
portfolios, candidate enumeration/growth, transitions, complete external cost,
cache and resumption, and selected-plan regret against an exhaustive small
oracle. Candidate/time/memory caps produce an explicit bounded-failure result,
never a silently truncated optimum. Eleven raw runs use the benchmark mutex.
