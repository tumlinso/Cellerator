# Representative profile benchmark v1

Profile build, exact scans, sketches, load, mapped queries, state transfer,
branch joins, multi-state alternatives, and peak memory are measured separately.
Every estimator is paired with an exact small-data calculation and reports
absolute and relative error. Eleven raw samples and mapped/copy residency are
retained under the benchmark mutex; profile identity and hash are mandatory.
