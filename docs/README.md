# Cellerator Umbrella Docs

This directory is the documentation surface for the new Cellerator umbrella
workspace. Historical Cellerator docs are preserved under
`Archive/current-cellerator/docs/` until their content is reclassified into
component-specific documentation.

For implementation ownership, start with the root `README.md` and `AGENTS.md`.
