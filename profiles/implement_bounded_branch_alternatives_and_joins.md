# Bounded profile alternatives v1

Compatible branch states remain deterministically ordered up to an explicit
limit. Above it, one conservative state widens ranges, keeps the lowest
confidence, and discards state/evidence identity rather than cloning code
exponentially. Mismatched semantic subjects never join.
