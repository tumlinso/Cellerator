# Semantic profile transfers v1

Every current operation family declares whether values, support, structure,
order, and generation are preserved, derived, invalidated, or unknown. Native
publication contracts invalidate only the layers they publish; opaque calls
widen all layers to unknown. Transfer functions alter evidence state, never the
underlying operation semantics.
