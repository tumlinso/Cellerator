# Profile selection and binding v1

Build/driver-provided artifacts bind stable symbol and field identities only
after domain and axis identity match. A requested name or explicit default picks
the state. Multiple matches are ambiguous, generic reference profiles require
permission, activated semantics reject total absence, and pure C++ falls through
without acquiring profile semantics.
