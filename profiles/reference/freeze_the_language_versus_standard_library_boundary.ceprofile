profile language_stdlib_boundary_v1 {
  base_language = [opt_in, domain, axis, order, state, relation_transfer,
                   operation, field, effect, exact_coverage, profile,
                   planning_authority, identity_generation, ceir, native];
  stdlib = [owner, view, binder, builder, container, reorder, canonicalize,
            bundle, exchange, common_biology, algorithm, policy, helper];
  implicit_unsafe = false;
}
