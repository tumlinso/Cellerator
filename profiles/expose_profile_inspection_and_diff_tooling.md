# Profile inspection and diff API v1

Allocation-free formatting APIs expose stable summaries and diffs in human or
machine JSON form. Outputs show confidence, ranges, evidence revision, expected
mutation masks, and missing-evidence masks. The central driver may surface these
APIs as subcommands during integration without moving profile semantics into it.
