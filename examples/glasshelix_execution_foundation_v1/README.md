# Prospective separate Cellerator consumer

This ordinary C++20 translation unit links the actual future Cellerator native foundation. It does not depend on GlassHelix, include implementation `.cu` files, or provide a fake backend. The example evaluates a genuine three-input nonlinear expression for 33 instances, reuses one prepared structure with independent gain planes, and checks a nonzero parameter derivative at zero primal gain.

The API header under the planning package's `contracts/` is a declaration-only sketch. Its spelling can change through the developmental interface tasks; the mathematical and ownership assertions cannot be removed to make the demo pass. Normal CMake configuration requires the real future target and fails without it. The package creation performs a syntax-only check against the guarded sketch, not linking or CUDA execution.

`Cellerator::native_foundation` is a proposed focused consumer target, not a current capability claim. The controller should expose existing core owners through the smallest coherent target rather than create a second implementation for this example.
