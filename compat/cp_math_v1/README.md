# CP-Math v1 evidence

This directory preserves the first-generation request, structural planning,
BELL lowering, packed-dense, and referee implementation for reproducibility.
It is not Cellerator's operation-core, planner, or runtime authority. Current
execution code must not add dependencies on this directory.
