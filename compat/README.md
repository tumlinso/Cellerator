# Compatibility

This tree preserves explicitly historical APIs and reproducibility evidence.
It is not an alternative implementation architecture. Current Cellerator code
must not acquire new dependencies on targets defined here.
