#pragma once

// Compatibility umbrella for the original single-scale matrix surface.
// It currently resolves to the quantized microscaled backend, so including
// this header adds no extra runtime layer.
#include "quantizedMatrix.cuh"
