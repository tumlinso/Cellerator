# `compute/neighbors`

Compute-facing home for neighbor search operators.

Current state:

- the public compute include surface lives here
- implementation is still backed by the existing `_rna/_neighbors` code until it is split into smaller operators

Do not add new reusable neighbor code under `_rna`.
