# `compute/neighbors`

Compute-facing home for neighbor search operators.

The cuVS sharded KNN implementation and the forward-neighbor index now both live here.

Do not add new reusable neighbor code under `_rna`.
