# `compute/neighbors/scoring`

Reusable neighbor scoring and merge math.

Caller policy belongs in CellShardNeighbors. Keep only distance/similarity,
row-normalization, candidate merge, and refine kernels here.
