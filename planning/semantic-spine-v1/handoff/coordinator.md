# Coordinator handoff: CE-SS1-RUN-V1

**This is an inert future execution prompt. Package construction and manual plan application do not execute it.**

After separate explicit implementation authorization, inspect the live run and verify the native plan was applied without collisions. Preserve the Cellerator authority UUID. Bind first-class Codex agents to the seven declared lanes using the installed workflow. Use the canonical `next_task`, `inspect_task`, `coordinate_task`, and `finish_task` operations. `delegate_task` is a subordinate local worker and must not be used to simulate first-class lanes.

Run I01 and the CORE queue through the tested I02 foundation. Publish the internal interface and integrated base. Release NATIVE, FRONTEND, ALGEBRA and VERIFY concurrently once each workspace contains that exact base; let I03 proceed independently. Do not serialize those four queues by waiting for one lane to finish before dispatching another. Do not require final GPU completion before releasing tests or compiler work that only needs the shared interface.

Maintain narrow file ownership. Shared header edits require coordinated revision and consumer invalidation. The integrator alone owns root/central build edits. Collect committed source artifacts and real evidence, integrate at I04, then execute the integrated gates at I05/I06/I07. Resolve any installed artifact-ready/integration-state distinction through canonical coordination rather than fabricated completion or database edits.

Acquire real canonical GPU leases for baseline, native and final hardware work. Pin a suitable CUDA12.x sm70 build lane. GPU absence, sanitizer absence or failed numerical evidence blocks required acceptance; it is not a skipped successful completion. CPU oracle tests are necessary but not the product demonstration.

Complete this bounded epic only: forward+transpose, canonical semantics, two value generations, false semantic mappings repaired, content-preserving .ceh conversion, and the supplied small demo. No general compiler/SDK/algebra/JIT/geometry/performance campaign. Full .cell execution and semantic cross-TU behavior remain visibly deferred.

Commit and push after each completed Todo through the configured repository workflow, integrate only this epic’s scoped work, preserve unrelated branches and valuable code, and report any failed push honestly. Pause immediately on the user’s request. Close the root only after all29 leaves and the actual I07 evidence. Then stop. Never auto-start a successor program.
