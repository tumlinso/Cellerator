# CE-SS1-L-INTEGRATE

**Inert execution handoff. Do not start from package construction or plan application alone.**

Role: integrator. Workspace authority mode: `exclusive`. Run: `CE-SS1-RUN-V1`.

## Queue

1. `CE-SS1-I01`: Establish the bounded execution baseline and protected edit inventory
2. `CE-SS1-I02`: Integrate the foundation and release parallel lanes
3. `CE-SS1-I03`: Retire .ceh files without expanding the language milestone
4. `CE-SS1-I04`: Integrate all four lanes and repository-local targets
5. `CE-SS1-I05`: Run cross-origin semantic and real sm70 conformance
6. `CE-SS1-I06`: Run the supplied biological demo and CUDA memory checks
7. `CE-SS1-I07`: Close the limited epic and preserve deferred obligations

## Required practice

Read the scope, contract and task sheet. Bind this first-class Codex agent to the exact run/lane through the installed canonical workflow; do not substitute a local `delegate_task` worker. Use the lane queue and declared dependencies, not a global task grab.

Only this lane owns central build wiring. Publish the actual integrated foundation before fan-out; collect committed artifacts as they become ready and join before final tests. Preserve unread user .ceh content as .cell or justify redundant removal. Use real executable gates, not metadata claims.

Respect exact file ownership. Request a coordinated contract revision before changing shared signatures. Record actual commands/results, commit each completed task, and push through the configured workflow without inventing a successful push. Pause promptly when asked. Stop after the assigned queue; do not launch another epic.
